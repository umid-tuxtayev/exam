class catigoriaDto {
    constructor({ name, _id, model_image, model_imageId }) {
        this.name = name
        this._id = _id
        this.model_image = model_image
        this.model_imageId = model_imageId
    }
};

module.exports = catigoriaDto;
